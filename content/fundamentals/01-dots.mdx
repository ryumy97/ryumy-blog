---
title: "점(Dots) - 데이터 시각화의 기본 요소"
description: "점은 데이터 시각화에서 가장 기본적인 요소입니다. 개별 데이터 포인트를 표현하는 강력한 방법을 알아보세요."
date: "2024-01-15"
category: "fundamentals"
tags: ["점", "기본요소", "산점도", "시각화"]
image: "/images/dots-visualization.jpg"
codesandbox: "dots-example"
order: 1
---

# 점(Dots) - 데이터 시각화의 기본 요소

점은 데이터 시각화에서 가장 기본적이면서도 강력한 요소입니다. 개별 데이터 포인트를 표현하는 가장 직관적인 방법 중 하나로, 복잡한 데이터셋에서도 패턴과 관계를 명확하게 보여줄 수 있습니다.

## 점의 기본 개념

점은 2차원 또는 3차원 공간에서 개별 데이터 포인트를 나타내는 기하학적 요소입니다. 각 점은 하나의 관찰값이나 측정값을 표현하며, 점들의 분포를 통해 데이터의 특성을 파악할 수 있습니다.

### 점의 장점

- **단순성**: 이해하기 쉽고 직관적
- **확장성**: 많은 수의 데이터 포인트도 표현 가능
- **유연성**: 크기, 색상, 모양 등 다양한 속성으로 정보 전달
- **효율성**: 공간을 효율적으로 활용

## 실제 구현 예제

다음은 React와 D3.js를 사용한 기본적인 점 시각화 예제입니다:

```jsx
import React, { useEffect, useRef } from "react";
import * as d3 from "d3";

const DotVisualization = ({ data }) => {
  const svgRef = useRef();

  useEffect(() => {
    if (!data || !svgRef.current) return;

    const svg = d3.select(svgRef.current);
    const width = 600;
    const height = 400;
    const margin = { top: 20, right: 20, bottom: 30, left: 40 };

    // Clear previous content
    svg.selectAll("*").remove();

    // Create scales
    const xScale = d3
      .scaleLinear()
      .domain([0, d3.max(data, (d) => d.x)])
      .range([margin.left, width - margin.right]);

    const yScale = d3
      .scaleLinear()
      .domain([0, d3.max(data, (d) => d.y)])
      .range([height - margin.bottom, margin.top]);

    // Add dots
    svg
      .selectAll("circle")
      .data(data)
      .enter()
      .append("circle")
      .attr("cx", (d) => xScale(d.x))
      .attr("cy", (d) => yScale(d.y))
      .attr("r", 5)
      .attr("fill", "#3B82F6")
      .attr("opacity", 0.7)
      .on("mouseover", function () {
        d3.select(this).attr("r", 8).attr("fill", "#EF4444");
      })
      .on("mouseout", function () {
        d3.select(this).attr("r", 5).attr("fill", "#3B82F6");
      });

    // Add axes
    svg
      .append("g")
      .attr("transform", `translate(0,${height - margin.bottom})`)
      .call(d3.axisBottom(xScale));

    svg
      .append("g")
      .attr("transform", `translate(${margin.left},0)`)
      .call(d3.axisLeft(yScale));
  }, [data]);

  return (
    <svg
      ref={svgRef}
      width="600"
      height="400"
      style={{ border: "1px solid #ccc" }}
    />
  );
};

export default DotVisualization;
```

## 점의 다양한 활용

### 1. 산점도 (Scatter Plot)

가장 일반적인 점 시각화로, 두 변수 간의 관계를 보여줍니다.

### 2. 버블 차트 (Bubble Chart)

점의 크기를 세 번째 변수로 활용하여 더 많은 정보를 전달합니다.

### 3. 점 지도 (Dot Map)

지리적 위치에 점을 배치하여 공간적 분포를 시각화합니다.

### 4. 점 플롯 (Dot Plot)

범주형 데이터의 분포를 점의 개수로 표현합니다.

## 디자인 고려사항

### 점의 크기

- 너무 작으면 보이지 않음
- 너무 크면 겹침 현상 발생
- 데이터 포인트 수에 따라 적절한 크기 조정

### 점의 색상

- 의미 있는 색상 매핑
- 접근성을 고려한 색상 선택
- 일관된 색상 체계 유지

### 점의 투명도

- 겹침 현상 완화
- 밀도 표현
- 시각적 혼잡도 감소

## 마무리

점은 데이터 시각화의 기본 요소이지만, 그 활용법은 무궁무진합니다. 적절한 디자인과 상호작용을 통해 단순한 점들이 강력한 인사이트를 전달할 수 있습니다.

다음 포스트에서는 점을 연결하는 **선(Line)**에 대해 알아보겠습니다.
